from setuptools import setup

setup(name='visearch',
      install_requires=[
          'pymongo',
          'pandas',
          'unipath',
          'xlrd',
          'python-django',
          'nltk',
      ],
      )
